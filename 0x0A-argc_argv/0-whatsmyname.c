#include <stdio.h>
#include "main.h"

/**
 * main - prints the name of the program then new line
 * @argc: number of arg
 * @argv: array of arg
 * Return: Always 0 
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}

