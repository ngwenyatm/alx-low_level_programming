#include "main.h"
/**
 * abs - computes the absolute value of an integrer
 *
 * @n: the integer
 *
 * Return: the absolute value of the integer
 *
 */

int abs(int n)
	{
		if (n >= 0)
		{
			return (n);
		}
		return (-n);
	}

